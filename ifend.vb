Imports System
Module Module1
    Sub Main()
        Dim str As String
        str = Console.ReadLine()
        If (str = "1") Then
            Console.WriteLine("We're number 1!")
        End If
    End Sub
End Module ' If.vb
