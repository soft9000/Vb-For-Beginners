
Imports System
Module Module1
    Sub Main()
        Dim iTotal, ss As Integer
        Dim bAdding As Boolean
        Dim sInput As String
        Dim cmdArgs As String() = _
            Environment.GetCommandLineArgs()
        For ss = 1 to cmdArgs.length - 1
            ' Use Select / Case / End Select Here ...
        Next
        Console.Write(" > The result is ")
        Console.WriteLine(iTotal)
    End Sub
End Module ' fmcalc_setup.vb
